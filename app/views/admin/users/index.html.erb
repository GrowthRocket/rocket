<table class="table table-bordered">
    <thead>
      <tr>
        <td>#</td>
        <td>用户名</td>
        <td>创建时间</td>
      </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
    <tr>
      <td> <%= user.id %></td>
      <td> <%= user.email %></td>
      <td><%= user.created_at %></td>
    </tr>
    <% end %>
  </tbody>
  </table>
