<%= simple_form_for [:admin, @project] do |f| %>

  <%= f.input :name, :label =>"项目名称" %>
  <%= f.input :description, :label =>"项目描述" %>

  <%= f.input :image, :label =>"主题图片" %>

  <% if @savetype == 1 %>
    <%= f.submit "创建项目", class: "btn btn-info" %>
  <% else %>
    <%= f.submit "编辑项目", class: "btn btn-success" %>
  <% end %>
<% end %>
