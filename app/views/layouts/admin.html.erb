<!DOCTYPE html>
<html>
    <head>
        <title>管理员面板</title>
        <%= csrf_meta_tags %>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    </head>

    <body style="background-image: url(/images/Body_bg.png);">

        <div class="container-fluid">
            <%= render "common/navbar" %>
            <div class="row">
            <%= render "common/flashes" %>
              <div class="col-md-8 col-md-offset-2">
                <!-- <ul class="nav nav-tabs nav-stacked" style="max-width: 300px; padding-top:20px;">
                   <li class="active"><%= link_to("众筹项目管理", admin_projects_path,) %> </li>
                   <li class="active"><%= link_to("用户订单管理", admin_orders_path,) %> </li>
                   <li class="active"><%= link_to("用户管理",  admin_users_path, ) %> </li>
                </ul> -->
                    <ul class="nav nav-tabs">
                        <li role="presentation" class="active"><%= link_to("众筹项目管理", admin_projects_path,) %> </li>
                        <li role="presentation" class="active"><%= link_to("订单管理", admin_orders_path,) %></li>
                        <li role="presentation" class="active"><%= link_to("用户管理",  admin_users_path, ) %></li>
                    </ul>
              </div>

            </div>
            <div class="row">
              <div class="col-md-8 col-md-offset-2">
                <%= yield %>
              </div>
        </div>

        <%= render "common/footer" %>

    </body>
</html>
